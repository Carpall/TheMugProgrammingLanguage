const main = fn {
	let n: u32 = 0 // unreachable from fib: fn(u32): u32
	
	const fib = fn (n: u32): u32 {
		if n < 2 { n } else {
			fib(n - 1) + fib(n - 2)
		}
	}
	
	println!("fib({}): {}", n, fib(n))
}