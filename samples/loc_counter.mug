const std = import!("std")

const LinesCounter = struct {
  let mut count: usize = 0
  let text: str

  const Self = LinesCounter

  pub const get_count = fn(self: Self): usize { self.count }

  
  pub const from = fn(text: str): Self { new { text: text } }
}

const main = fn {
  let mut counter: LinesCounter
  let files = collect_all_files

  for 
}