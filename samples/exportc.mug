|exportc: "mug_add"|
export const add = fn(a, b: i32): i32 { a + b }

test "add(1, 2)" {
  assert!(add(1, 2) == 3)
}

test "add(-1, -1)" {
  assert!(add(-1, -1) == -2)
}

test "add(0, 0)" {
  assert!(add(0, 0) == 0)
}