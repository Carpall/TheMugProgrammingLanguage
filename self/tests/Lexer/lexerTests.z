type LexerTests {
  lexer: Lexer

  pub fn keywords(&self: LexerTests): TestResponse {
    TestResponse.from(true, "nothing broken")
  }
}

[test]
fn lexerTests() {
  testsFor(static node!(LexerTests))
}