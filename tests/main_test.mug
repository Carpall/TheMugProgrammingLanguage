func fib(n: i32): i32 {
  var last = 1
  var prelast = 0
  for var i = n, i >= 0, i-- {
    const tmplast = last
    last += prelast
    prelast = tmplast
  }

  last
}

func main() {
  exit!(fib(44))
}