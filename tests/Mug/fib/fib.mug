fun fib(n: i32): i32 {
  var last = 1
  var prelast = 0
  for var i = n, i >= 0, i-- {
    const tmp = last + prelast
    prelast = last
    last = tmp
  }

  last
}

fun main() {
  var result = 0

  for var i = 0, i <= 40, i++ { result += fib(i) }
  
  exit!(result)
}
